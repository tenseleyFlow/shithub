// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: relays.sql

package webhookrelaydb

import (
	"context"
)

const createRelay = `-- name: CreateRelay :one

INSERT INTO user_webhook_relays (
    user_id, name, token_hash, token_prefix,
    hmac_secret_ciphertext, hmac_secret_nonce, destinations
)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, user_id, name, token_hash, token_prefix,
          hmac_secret_ciphertext, hmac_secret_nonce, destinations,
          disabled_at, created_at, updated_at
`

type CreateRelayParams struct {
	UserID               int64
	Name                 string
	TokenHash            []byte
	TokenPrefix          string
	HmacSecretCiphertext []byte
	HmacSecretNonce      []byte
	Destinations         []byte
}

// SPDX-License-Identifier: AGPL-3.0-or-later
func (q *Queries) CreateRelay(ctx context.Context, db DBTX, arg CreateRelayParams) (UserWebhookRelay, error) {
	row := db.QueryRow(ctx, createRelay,
		arg.UserID,
		arg.Name,
		arg.TokenHash,
		arg.TokenPrefix,
		arg.HmacSecretCiphertext,
		arg.HmacSecretNonce,
		arg.Destinations,
	)
	var i UserWebhookRelay
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.TokenHash,
		&i.TokenPrefix,
		&i.HmacSecretCiphertext,
		&i.HmacSecretNonce,
		&i.Destinations,
		&i.DisabledAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteRelay = `-- name: DeleteRelay :exec
DELETE FROM user_webhook_relays WHERE id = $1
`

func (q *Queries) DeleteRelay(ctx context.Context, db DBTX, id int64) error {
	_, err := db.Exec(ctx, deleteRelay, id)
	return err
}

const disableRelay = `-- name: DisableRelay :exec
UPDATE user_webhook_relays
SET disabled_at = now(), updated_at = now()
WHERE id = $1
`

func (q *Queries) DisableRelay(ctx context.Context, db DBTX, id int64) error {
	_, err := db.Exec(ctx, disableRelay, id)
	return err
}

const getRelayByID = `-- name: GetRelayByID :one
SELECT id, user_id, name, token_hash, token_prefix,
       hmac_secret_ciphertext, hmac_secret_nonce, destinations,
       disabled_at, created_at, updated_at
FROM user_webhook_relays
WHERE id = $1
`

func (q *Queries) GetRelayByID(ctx context.Context, db DBTX, id int64) (UserWebhookRelay, error) {
	row := db.QueryRow(ctx, getRelayByID, id)
	var i UserWebhookRelay
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.TokenHash,
		&i.TokenPrefix,
		&i.HmacSecretCiphertext,
		&i.HmacSecretNonce,
		&i.Destinations,
		&i.DisabledAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getRelayByTokenHash = `-- name: GetRelayByTokenHash :one
SELECT id, user_id, name, token_hash, token_prefix,
       hmac_secret_ciphertext, hmac_secret_nonce, destinations,
       disabled_at, created_at, updated_at
FROM user_webhook_relays
WHERE token_hash = $1
`

func (q *Queries) GetRelayByTokenHash(ctx context.Context, db DBTX, tokenHash []byte) (UserWebhookRelay, error) {
	row := db.QueryRow(ctx, getRelayByTokenHash, tokenHash)
	var i UserWebhookRelay
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.TokenHash,
		&i.TokenPrefix,
		&i.HmacSecretCiphertext,
		&i.HmacSecretNonce,
		&i.Destinations,
		&i.DisabledAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listRelaysForUser = `-- name: ListRelaysForUser :many
SELECT id, user_id, name, token_hash, token_prefix,
       hmac_secret_ciphertext, hmac_secret_nonce, destinations,
       disabled_at, created_at, updated_at
FROM user_webhook_relays
WHERE user_id = $1
ORDER BY created_at DESC
`

func (q *Queries) ListRelaysForUser(ctx context.Context, db DBTX, userID int64) ([]UserWebhookRelay, error) {
	rows, err := db.Query(ctx, listRelaysForUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []UserWebhookRelay{}
	for rows.Next() {
		var i UserWebhookRelay
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.TokenHash,
			&i.TokenPrefix,
			&i.HmacSecretCiphertext,
			&i.HmacSecretNonce,
			&i.Destinations,
			&i.DisabledAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
